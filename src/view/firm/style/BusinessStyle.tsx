import styled from 'styled-components';
import { FONT_SIZE, FONT_WEIGHT } from '../../../../styles/BaseStyle';

export const BusinessTypeRow = styled.tr`
  td.business-type-list {
    padding: 0;
  }
`;

export const BusinessTypeListTable = styled.table`
  .MuiTableCell-root {
    font-size: ${FONT_SIZE};
    font-weight: ${FONT_WEIGHT};
  }
`;
